.box
  .cell
    = build_navigation([link_to(@user.nickname,member_path(@user.nickname))])
  .cell
    = render :partial => 'topics/profile_topic', :collection => @topics, :as => :topic, :locals => {:topic_user => @user}
  = paginate @topics if @topics.any?

